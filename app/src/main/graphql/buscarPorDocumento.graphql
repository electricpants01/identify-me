query getUserDocument($ci: String!, $doc: String!, $nom: String!, $key: String!) {
    buscaPorDocumento(_ci: $ci, _doc: $doc, _nom: $nom, _key: $key) {
        numdoc
        nombres
        appaterno
        apmaterno
        fnacimiento
        foto
        respuesta
    }
}
